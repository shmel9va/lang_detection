# Lazy import — LanguageDetector не импортируется автоматически,
# чтобы не тянуть fasttext при загрузке только классификаторов.
# Используй явный импорт:
#   from scripts.detection.detector import LanguageDetector
